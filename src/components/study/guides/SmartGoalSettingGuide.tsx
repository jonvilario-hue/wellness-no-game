
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function SmartGoalSettingGuide() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>SMART Goal Setting</CardTitle>
        <CardDescription>Creating effective, actionable goals.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Explanation of benefits...</p>
      </CardContent>
    </Card>
  );
}
