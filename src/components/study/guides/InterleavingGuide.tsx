
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function InterleavingGuide() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Interleaving</CardTitle>
        <CardDescription>Mixing different topics to improve learning.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Explanation of benefits...</p>
      </CardContent>
    </Card>
  );
}
