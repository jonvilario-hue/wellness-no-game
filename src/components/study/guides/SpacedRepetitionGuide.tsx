
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function SpacedRepetitionGuide() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Spaced Repetition</CardTitle>
        <CardDescription>Reviewing information at increasing intervals.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Explanation of benefits...</p>
      </CardContent>
    </Card>
  );
}
